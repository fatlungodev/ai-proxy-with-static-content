services:
  ai-proxy:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: ai-proxy
    restart: unless-stopped
    ports:
      - "3005:3005"
    env_file:
      - ../.env
